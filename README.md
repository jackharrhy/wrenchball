# Sluggers Super Draft

## setting up

start the database:

```shell
cd website
docker compose up db
```

start the web server:

```shell
cd website
npm run dev
```

if there are missing packages:

```shell
cd website
npm install
npm run dev
```
